So basically i have a wireshark packets file

i opened it and saw a couple of packets. 
I filtered all the packets by the length and saw one packet that was different length from the others: `123 bytes`

I did that because i tought the flag could be in a different bytes amount o the other

than i got the flag

i also ran `strings traffic.pcapng` and saw the flag there.